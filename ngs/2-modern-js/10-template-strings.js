const greeting = "Hello World";

const answer = 'Forty Two';

/*
Template string:
- inject values of an expression directly into strings
- can also be multi-line strings
*/
const html = `
  <div>
    ${Math.random()}
  </div>
`;
