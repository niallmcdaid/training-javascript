// Scalar values
// These values are constant can cannot change
const answer = 42;
const greeting = 'Hello';

// Arrays and Objects
// The arrays/objects defined will always point to the same array/object but the contents of these CAN change

const numbers = [2, 4, 6];
const person = {
  firstName: 'John',
  lastName: 'Doe',
};


// It is the reference that is constant, not the value.